#include <stdio.h>
int  main() {

    int n;//number the user inputs
    while(scanf("%d", &n) !=EOF){ //scans until end of file
    printf("%d\n", 3*n+7); //spits out the function's result
    }
}

